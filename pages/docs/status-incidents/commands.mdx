---
title: Commands | Status Incidents
description: A detailed overview of commands available in Status Incidents
---

import { useData } from 'nextra/data'

import CommandAccordion from '@components/Commands/CommandAccordion'

export const getStaticProps = ({ params }) => {
  return fetch(`${process.env.NEXT_PUBLIC_STATUS_INCIDENTS_API_URL}/api/commands`)
    .then((res) => res.json())
    .then(data => ({
      props: {
        // We add an `ssg` field to the page props,
        // which will be provided to the Nextra `useData` hook.
        ssg: {
          commands: data
        }
      },
      // The page will be considered as stale and regenerated every hour.
      revalidate: 3600
    }))
    .catch((err) => ({
      props: {
        ssg: {
          commands: []
        }
      }
    }))
}

export const StatusIncidentsCommandAccordion = () => {
  const { commands = [] } = useData()
  return <CommandAccordion commands={commands}/>
}
 
# Commands

<StatusIncidentsCommandAccordion />
